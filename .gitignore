# session holds your access_token — never commit it
session.local.json
.env

# python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.venv/
venv/

# apks / re artifacts
*.apk
*.xapk
*.so
apks/

# frida: node deps + build/scratch artifacts (keep the .js sources + runners)
frida/node_modules/
frida/package-lock.json
frida/*.c.js
frida/_*.log
frida/_*.png
frida/.pid
